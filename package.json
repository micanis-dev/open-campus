{
  "name": "frontend",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "deploy": "pnpm build && wrangler pages deploy dist --project-name ai-system-open-campus --branch main",
    "dev": "slidev --open",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "^52.19.1",
    "@slidev/theme-default": "latest",
    "pyodide": "^314.0.6",
    "vue": "^3.5.33"
  },
  "devDependencies": {
    "playwright-chromium": "^1.62.1",
    "wrangler": "^4.126.0"
  }
}
